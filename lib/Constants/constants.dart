import 'package:flutter/material.dart';

const String splashText =
    "Map out Multiple Workflows for all your internal process, always knows the status of your task";

enum TaskStatusEnum { todo, inProgress, completed }
